<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>betSA | Home</title>

  <!-- Bootstrap CSS -->
  <link
    href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
    rel="stylesheet"
  />

  <style>
    body {
      background: linear-gradient(to right, #002b5b, #0466c8);
      color: white;
      min-height: 100vh;
      font-family: "Poppins", sans-serif;
    }

    .navbar {
      background-color: rgba(0, 0, 0, 0.3);
      backdrop-filter: blur(10px);
    }

    .hero {
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      text-align: center;
      height: 80vh;
    }

    .hero h1 {
      font-size: 3.5rem;
      font-weight: 700;
    }

    .hero h2 {
      margin-top: 1rem;
      font-weight: 500;
    }

    .balance-box {
      background: rgba(255, 255, 255, 0.1);
      border-radius: 10px;
      padding: 1rem 2rem;
      margin: 1.5rem 0;
      display: inline-block;
    }

    .btn-custom {
      background-color: #ffc107;
      color: #000;
      border: none;
      font-weight: 600;
      transition: 0.3s ease;
    }

    .btn-custom:hover {
      background-color: #e0a800;
      color: #fff;
    }

    footer {
      text-align: center;
      padding: 1rem;
      background-color: rgba(0, 0, 0, 0.2);
      position: fixed;
      width: 100%;
      bottom: 0;
      color: #ccc;
    }
  </style>
</head>

<body>
  <!-- Navbar -->
  <nav class="navbar navbar-expand-lg navbar-dark">
    <div class="container">
      <a class="navbar-brand fw-bold" href="#">betSA</a>
      <div class="d-flex">
        <form action="/" method="GET">
          <button type="submit" class="btn btn-outline-light">Logout</button>
        </form>
      </div>
    </div>
  </nav>

  <!-- Hero Section -->
  <div class="hero container">
    <h1>Welcome to betSA</h1>
    <h2>Hello, <%= content.username %> 👋</h2>
    <div class="balance-box">
      <p class="mb-0">Available Balance</p>
      <h3>R <%= content.balance %></h3>
    </div>

    <div class="d-flex flex-column flex-md-row gap-3 mt-4">
      <form action="/dashboard" method="GET">
        <button type="submit" class="btn btn-custom btn-lg w-100">View All Events</button>
      </form>

      <form action="/viewSlip" method="GET">
        <button type="submit" class="btn btn-light btn-lg w-100">Show Bet Slip</button>
      </form>
    </div>
  </div>

  <footer>
    &copy; <%= new Date().getFullYear() %> betSA — Your trusted betting platform.
  </footer>

  <!-- Bootstrap JS -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
