<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>betSA | Login</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body {
      background: linear-gradient(135deg, #0d6efd, #6610f2);
      min-height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
      font-family: 'Poppins', sans-serif;
    }

    .login-card {
      background: white;
      border-radius: 15px;
      box-shadow: 0 10px 25px rgba(0, 0, 0, 0.1);
      width: 100%;
      max-width: 420px;
      padding: 2rem;
      text-align: center;
    }

    .login-card h2 {
      color: #0d6efd;
      margin-bottom: 1.5rem;
      font-weight: 700;
    }

    .form-control {
      border-radius: 10px;
      padding: 10px;
    }

    .btn-custom {
      border-radius: 10px;
      font-weight: 600;
    }

    .admin-btn {
      background-color: #6c757d;
      border: none;
    }

    .admin-btn:hover {
      background-color: #5a6268;
    }

    .message {
      color: red;
      margin-top: 1rem;
    }

    .logo {
      font-size: 2rem;
      font-weight: 800;
      color: #0d6efd;
      margin-bottom: 1rem;
    }
  </style>
</head>
<body>
  <div class="login-card">
    <div class="logo">betSA</div>
    <h2>Login</h2>
    <form action="/loginDetails" method="POST">
      <div class="mb-3 text-start">
        <label for="email" class="form-label">Email</label>
        <input type="email" id="email" name="email" class="form-control" required>
      </div>

      <div class="mb-3 text-start">
        <label for="password" class="form-label">Password</label>
        <input type="password" id="password" name="password" class="form-control" required>
      </div>

      <button type="submit" class="btn btn-primary w-100 btn-custom mb-3">Log In</button>
    </form>

    <form action="/adminLogin" method="GET">
      <button type="submit" class="btn admin-btn w-100 btn-custom">Login as Administrator</button>
    </form>

    <% if (locals.message) { %>
      <p class="message"><%= message %></p>
    <% } %>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
