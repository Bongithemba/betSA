<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>betSA | Register</title>

  <!-- Bootstrap CSS -->
  <link
    href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
    rel="stylesheet"
  />
  <link href="styles/registrationStyles.css" rel="stylesheet"/>
</head>

<body>
  <!-- Navbar -->
  <nav class="navbar navbar-expand-lg navbar-dark">
    <div class="container">
      <a class="navbar-brand fw-bold" href="#">betSA</a>
    </div>
  </nav> <style>

  </style>

  <!-- Registration Card -->
  <div class="auth-container">
    <div class="card shadow-lg">
      <div class="row g-0">
        <!-- Registration Form -->
        <div class="col-md-7">
          <div class="card-body">
            <h2 class="register-header">Create Your Account</h2>
            <form action="/register" method="POST">
              <div class="mb-3">
                <label for="username" class="form-label">Full Name</label>
                <input
                  type="text"
                  class="form-control"
                  id="username"
                  name="username"
                  required
                />
              </div>

              <div class="mb-3">
                <label for="email" class="form-label">Email Address</label>
                <input
                  type="email"
                  class="form-control"
                  id="email"
                  name="email"
                  required
                />
              </div>

              <div class="mb-3">
                <label for="password" class="form-label">Password</label>
                <input
                  type="password"
                  class="form-control"
                  id="password"
                  name="password"
                  required
                />
              </div>

              <div class="mb-3">
                <label for="retypePassword" class="form-label">Retype Password</label>
                <input
                  type="password"
                  class="form-control"
                  id="retypePassword"
                  name="retypePassword"
                  required
                />
              </div>

              <button type="submit" class="btn btn-custom mt-3">Register</button>

              <% if (locals.message) { %>
              <p class="error-message mt-3"><%= message %></p>
              <% } %>
            </form>
          </div>
        </div>

        <!-- Sign-in Section -->
        <div class="col-md-5 signin-section">
          <h3>Already have an account?</h3>
          <p>Log in to continue placing your bets!</p>
          <form id="loginform" action="/login" method="GET">
            <button type="submit">Sign In</button>
          </form>
        </div>
      </div>
    </div>
  </div>

  <footer>
    &copy; <%= new Date().getFullYear() %> betSA — Your trusted betting platform.
  </footer>

  <!-- Bootstrap JS -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
